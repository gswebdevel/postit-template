<% show_post ||= false %>

<div class='row'>
  <div class='span0 well text-center'>
    <% if current_user %>
        <%= link_to vote_post_comment_path(one_comment.post, one_comment, vote: true), method: 'post' do %>
            <i class="icon-arrow-up"></i>
            <% end %>
        <br/>
    <% end %>
    votes (<%= one_comment.total_votes %>)<br/>
    <% if current_user %>
        <%= link_to vote_post_comment_path(one_comment.post, one_comment, vote: false), method: 'post' do %>
            <i class="icon-arrow-down"></i>
            <% end %>
    <% end %>
  </div>

  <div class="well span8">
    <em>"<%= one_comment.body %>"</em>
    <% if show_post %>
        on <%= link_to one_comment.post.title, post_path(one_comment.post) %>
    <% end %>
    <br/>
    <span class="quiet">posted by <%= link_to one_comment.user.username, user_path(one_comment.user) %></span>
    <small>posted on <%= nice_date(one_comment.created_at) %>
    </small>

  </div>
</div>

